{
  "name": "moim",
  "version": "0.1.0",
  "private": true,
  "description": "Moim 모노레포 루트 — frontend + Cloudflare Workers",
  "scripts": {
    "dev": "echo Moim dev  -  frontend http://localhost:3000 && concurrently -n frontend,open -c magenta,gray \"npm:dev:frontend\" \"node scripts/open-browser.mjs http://localhost:3000\"",
    "dev:frontend": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "worker:dev": "wrangler dev",
    "worker:deploy": "npm run build && wrangler deploy",
    "db:schema": "wrangler d1 execute moim --local --file worker/schema.sql",
    "db:schema:remote": "wrangler d1 execute moim --remote --file worker/schema.sql"
  },
  "dependencies": {
    "jose": "^6.1.0"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "wrangler": "^4.121.0"
  }
}
