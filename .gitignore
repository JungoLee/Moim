# 의존성
node_modules/

# 환경변수 (시크릿)
.env
.env.local
.env.*.local

# 빌드 산출물
.next/
out/
dist/
build/

# Next 자동 생성
next-env.d.ts
*.tsbuildinfo

# 로그/OS
*.log
.DS_Store
Thumbs.db

# Claude Code 로컬 설정 (머신별 — 커밋하지 않음)
.claude/

# Cloudflare Workers 이관 (개인 데이터·로컬 시크릿 — 커밋 금지)
backup/
.dev.vars
.wrangler/
worker/seed.sql
